import React, { useEffect, useState } from "react";

const Dashboard = () => {
  const handleClick = (menuName) => {};
  return <div className="dashboard">current month sales</div>;
};

export default Dashboard;
